# Phase 11 WebP fixture

`sample-transparent.webp` is a 1254 × 1254, four-channel WebP generated locally from the project's transparent compression artwork. It is used for manual WebP decode, alpha-preservation, recompression, MIME, and signature checks.
