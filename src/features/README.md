# Features

Feature modules live here. Each module owns the user-facing behavior for one
product capability and should expose stable interfaces to the app layer.

Planned modules include local compression, direct image URLs, and website
scanning. They are intentionally not implemented in Phase 1.
