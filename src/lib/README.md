# Shared library

Pure utilities, configuration, and cross-feature services belong here. Browser
codec internals and worker protocols should be kept behind feature-facing
interfaces when those phases are implemented.
